name: Jobs Discovery and Search Ingest

on:
  workflow_dispatch:
    inputs:
      discover_write:
        description: "Append eligible discovered ATS sources to jobs/sources.json?"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      search_write:
        description: "Write search-ingested jobs to pending-synced-jobs.json?"
        required: true
        default: "true"
        type: choice
        options:
          - "false"
          - "true"
      candidates_file:
        description: "Path to the discovery candidate file (.json or .txt)."
        required: true
        default: "jobs/source-discovery-candidates.json"
        type: string
      search_sources_file:
        description: "Path to the search source config JSON."
        required: true
        default: "jobs/search-sources.json"
        type: string
  schedule:
    - cron: "15 13 * * 3"

permissions:
  contents: write

concurrency:
  group: jobs-board-state
  cancel-in-progress: false

jobs:
  discovery-and-search-ingest:
    runs-on: ubuntu-latest

    env:
      JOBS_DIR: resources/jobs
      CANDIDATES_FILE: ${{ github.event.inputs.candidates_file || 'jobs/source-discovery-candidates.json' }}
      SEARCH_SOURCES_FILE: ${{ github.event.inputs.search_sources_file || 'jobs/search-sources.json' }}
      GOOGLE_CUSTOM_SEARCH_API_KEY: ${{ secrets.GOOGLE_CUSTOM_SEARCH_API_KEY }}
      GOOGLE_CUSTOM_SEARCH_ENGINE_ID: ${{ secrets.GOOGLE_CUSTOM_SEARCH_ENGINE_ID }}
      SERPAPI_API_KEY: ${{ secrets.SERPAPI_API_KEY }}
      APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
      GENERIC_JOB_DATA_API_KEY: ${{ secrets.GENERIC_JOB_DATA_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install dependencies if needed
        shell: bash
        run: |
          if [ -f "$JOBS_DIR/package-lock.json" ] || [ -f "$JOBS_DIR/npm-shrinkwrap.json" ]; then
            npm --prefix "$JOBS_DIR" ci
          elif node -e "const p=require('./${JOBS_DIR}/package.json'); const hasDeps=Object.keys(p.dependencies||{}).length > 0 || Object.keys(p.devDependencies||{}).length > 0; process.exit(hasDeps ? 0 : 1)"; then
            npm --prefix "$JOBS_DIR" install
          else
            echo "No package dependencies declared. Skipping npm install."
          fi

      - name: Validate discovery and search scripts
        shell: bash
        run: |
          for file in \
            "$JOBS_DIR/scripts/discover-sources.js" \
            "$JOBS_DIR/scripts/search-ingest.js" \
            "$JOBS_DIR/scripts/source-discovery-helpers.js" \
            "$JOBS_DIR/scripts/sync-targeted-pending-sources.js" \
            "$JOBS_DIR/scripts/ats-clients.js"
          do
            if [ -f "$file" ]; then
              node --check "$file"
            else
              echo "Skipping missing optional script: $file"
            fi
          done

      - name: Guard and fix blocked sources before validation
        run: npm --prefix "$JOBS_DIR" run jobs:check-blocked-sources -- --write

      - name: Validate public data before automation
        run: npm --prefix "$JOBS_DIR" run jobs:validate

      - name: Run source discovery
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "schedule" ] || [ "${{ inputs.discover_write }}" != "true" ]; then
            npm --prefix "$JOBS_DIR" run jobs:discover-sources -- --candidates-file "$CANDIDATES_FILE" --queries-file "$SEARCH_SOURCES_FILE"
          else
            npm --prefix "$JOBS_DIR" run jobs:discover-sources-write -- --candidates-file "$CANDIDATES_FILE" --queries-file "$SEARCH_SOURCES_FILE"
          fi

      - name: Run search ingest
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "schedule" ] || [ "${{ inputs.search_write }}" = "true" ]; then
            npm --prefix "$JOBS_DIR" run jobs:search-ingest-write -- --queries-file "$SEARCH_SOURCES_FILE"
          else
            npm --prefix "$JOBS_DIR" run jobs:search-ingest -- --queries-file "$SEARCH_SOURCES_FILE"
          fi

      - name: Guard and fix blocked sources before validation
        run: npm --prefix "$JOBS_DIR" run jobs:check-blocked-sources -- --write

      - name: Validate public data after automation
        run: npm --prefix "$JOBS_DIR" run jobs:validate

      - name: Detect meaningful changes
        id: changes
        shell: bash
        run: |
          SOURCES_CHANGED="$(git status --porcelain -- "$JOBS_DIR/sources.json" || true)"
          CANDIDATES_CHANGED="$(git status --porcelain -- "$JOBS_DIR/source-discovery-candidates.json" || true)"
          PENDING_CHANGED="$(git status --porcelain -- "$JOBS_DIR/pending-synced-jobs.json" || true)"
          SOURCE_HEALTH_CHANGED="$(git status --porcelain -- "$JOBS_DIR/source-health-latest.json" || true)"
          REPORTS_CHANGED="$(git status --porcelain -- \
            "$JOBS_DIR/reports/source-discovery-report.json" \
            "$JOBS_DIR/reports/search-ingest-report.json" \
            "$JOBS_DIR/validation-snapshots/latest.json" \
            || true)"
          ERROR_SIGNAL="$(node - <<'NODE'
          const fs = require('fs');
          const root = process.env.JOBS_DIR || 'jobs';
          function readJson(file) {
            try {
              return JSON.parse(fs.readFileSync(file, 'utf8'));
            } catch (_) {
              return null;
            }
          }
          const searchReport = readJson(`${root}/reports/search-ingest-report.json`);
          const discoveryReport = readJson(`${root}/reports/source-discovery-report.json`);
          const searchError = Number(searchReport?.summary?.meaningful_error_count || 0) > 0;
          const discoveryError = Array.isArray(discoveryReport?.query_reports)
            && discoveryReport.query_reports.some((entry) => ['missing_env_vars', 'fetch_failed'].includes(String(entry?.status || '')));
          process.stdout.write(searchError || discoveryError ? 'true' : 'false');
          NODE
          )"
          CORE_CHANGED="false"
          REPORT_COMMIT="false"
          if [ -n "$SOURCES_CHANGED" ] || [ -n "$CANDIDATES_CHANGED" ] || [ -n "$PENDING_CHANGED" ]; then
            CORE_CHANGED="true"
            REPORT_COMMIT="true"
          elif [ "$ERROR_SIGNAL" = "true" ]; then
            REPORT_COMMIT="true"
          fi
          {
            echo "sources_changed=$([ -n "$SOURCES_CHANGED" ] && echo true || echo false)"
            echo "candidates_changed=$([ -n "$CANDIDATES_CHANGED" ] && echo true || echo false)"
            echo "pending_changed=$([ -n "$PENDING_CHANGED" ] && echo true || echo false)"
            echo "source_health_changed=$([ -n "$SOURCE_HEALTH_CHANGED" ] && echo true || echo false)"
            echo "reports_changed=$([ -n "$REPORTS_CHANGED" ] && echo true || echo false)"
            echo "core_changed=$CORE_CHANGED"
            echo "error_signal=$ERROR_SIGNAL"
            echo "should_commit_reports=$REPORT_COMMIT"
          } >> "$GITHUB_OUTPUT"
          echo "sources_changed=$SOURCES_CHANGED"
          echo "candidates_changed=$CANDIDATES_CHANGED"
          echo "pending_changed=$PENDING_CHANGED"
          echo "source_health_changed=$SOURCE_HEALTH_CHANGED"
          echo "reports_changed=$REPORTS_CHANGED"
          echo "error_signal=$ERROR_SIGNAL"

      - name: Upload discovery and ingest artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jobs-discovery-search-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            ${{ env.JOBS_DIR }}/sources.json
            ${{ env.JOBS_DIR }}/source-discovery-candidates.json
            ${{ env.JOBS_DIR }}/pending-jobs.json
            ${{ env.JOBS_DIR }}/pending-synced-jobs.json
            ${{ env.JOBS_DIR }}/source-health-latest.json
            ${{ env.JOBS_DIR }}/scrape-report.json
            ${{ env.JOBS_DIR }}/reports/source-discovery-report.json
            ${{ env.JOBS_DIR }}/reports/search-ingest-report.json
            ${{ env.JOBS_DIR }}/validation-snapshots/latest.json

      - name: Commit and push discovery/search changes
        if: steps.changes.outputs.core_changed == 'true' || steps.changes.outputs.should_commit_reports == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ "${{ steps.changes.outputs.sources_changed }}" = "true" ] && [ -e "$JOBS_DIR/sources.json" ]; then
            git add "$JOBS_DIR/sources.json"
          fi
          if [ "${{ steps.changes.outputs.candidates_changed }}" = "true" ] && [ -e "$JOBS_DIR/source-discovery-candidates.json" ]; then
            git add "$JOBS_DIR/source-discovery-candidates.json"
          fi
          if [ "${{ steps.changes.outputs.pending_changed }}" = "true" ] && [ -e "$JOBS_DIR/pending-synced-jobs.json" ]; then
            git add "$JOBS_DIR/pending-synced-jobs.json"
          fi
          if [ "${{ steps.changes.outputs.should_commit_reports }}" = "true" ]; then
            for path in \
              "$JOBS_DIR/pending-jobs.json" \
              "$JOBS_DIR/source-health-latest.json" \
              "$JOBS_DIR/scrape-report.json" \
              "$JOBS_DIR/reports/source-discovery-report.json" \
              "$JOBS_DIR/reports/search-ingest-report.json" \
              "$JOBS_DIR/validation-snapshots/latest.json"; do
              if [ -e "$path" ]; then
                git add "$path"
              else
                echo "Skipping missing path: $path"
              fi
            done
          fi
          if git diff --cached --quiet; then
            echo "No staged changes to commit."
            exit 0
          fi
          git commit -m "Run jobs discovery and search ingest"
          git push

      - name: Report no-op
        if: steps.changes.outputs.core_changed != 'true' && steps.changes.outputs.should_commit_reports != 'true'
        run: echo "No commit created."
