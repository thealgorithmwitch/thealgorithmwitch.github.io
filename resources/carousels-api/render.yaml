services:
  - type: web
    name: html-scryer-api
    env: node
    plan: free
    buildCommand: cd resources/carousels-api && npm install && npx puppeteer browsers install chrome
    startCommand: cd resources/carousels-api && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
