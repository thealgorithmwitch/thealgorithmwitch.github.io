# Talent Pipeline Audit

**Generated**: 2026-05-25T23:45:00Z

## Executive Summary

Two stale file references (`pending-talent.json`, `pending-talent-profiles.json`) are tracked in CI but **never generated by any script**. They should be removed from `jobs-sync.yml` to prevent confusion and useless artifact warnings. The canonical talent pipeline is clean — one file (`talent-profiles.json`) flows from backend → local → frontend.

---

## Canonical Files

### `talent-profiles.json` (CANONICAL)

| Attribute | Detail |
|-----------|--------|
| Path | `resources/jobs/talent-profiles.json` |
| Defined | `scripts/public-records.js:11` as `TALENT_PROFILES_FILE` |
| Generated by | `scripts/fetch-approved-talent.js` — merges approved profiles from GAS backend |
| Consumed by | `index.html` (public display), `admin-review.html` (active talent tab), `validate-public-data.js` (validation), `apply-admin-actions.js` (admin operations) |
| CI tracked | `jobs-sync.yml` lines 94 (git status), 130 (artifact), 157 (git add) — **keep these** |

### `scripts/fetch-approved-talent.js` (CANONICAL)

| Attribute | Detail |
|-----------|--------|
| Role | Fetches approved talent from GAS backend (`getApprovedTalent`) and merges into `talent-profiles.json` |
| Input | GAS backend (approved talent rows) + existing `talent-profiles.json` |
| Output | `talent-profiles.json` + `reports/talent-export-audit.json` |
| Invoked by | `npm run jobs:fetch-approved-talent` (package.json:7) → CI `jobs-sync.yml:68` |
| Note | Fetches `getPendingTalent` too but **only for logging** — never writes it |

---

## Stale References (Do NOT Exist)

### `pending-talent.json` — STALE REFERENCE

| Attribute | Detail |
|-----------|--------|
| Path | `resources/jobs/pending-talent.json` |
| On disk? | **NO** |
| Generated by? | **No script generates it.** `fetch-approved-talent.js:126` fetches it from GAS but only logs the count. |
| CI references | `jobs-sync.yml` lines **95** (git status), **131** (artifact), **158** (git add) |
| Should be removed? | **YES** — all 3 references. The pending queue is served in real-time from GAS by `admin-review.html`. |

### `pending-talent-profiles.json` — STALE REFERENCE

| Attribute | Detail |
|-----------|--------|
| Path | `resources/jobs/pending-talent-profiles.json` |
| On disk? | **NO** |
| Generated by? | **No script generates it.** |
| CI references | `jobs-sync.yml` lines **96** (git status), **132** (artifact), **159** (git add) |
| Should be removed? | **YES** — all 3 references. |

---

## `scripts/public-talent.js` — DOES NOT EXIST

No separate `public-talent.js` pipeline exists. Unlike jobs (which have `public-jobs.js`), talent profiles are consumed directly from `talent-profiles.json`. No remediation needed.

---

## Data Flow

```
TALENT SUBMISSION
  talent-apply.html ──POST──→ GAS backend (submitTalent)
                                   ↓
                            "Pending Talent" sheet
                                   ↓
ADMIN REVIEW (real-time from GAS)
  admin-review.html ──getPendingTalent──→ GAS ──→ Pending Talent sheet
                  ──approveTalent──────→ GAS ──→ Pending → Approved sheets
                  ──rejectTalent───────→ GAS ──→ Removes from Pending sheet
                                   ↓
CI SYNC (periodic pull)
  fetch-approved-talent.js ──getApprovedTalent──→ GAS ──→ Approved Talent sheet
                          ↓
                    merge into talent-profiles.json ←── (preserves existing fields)
                                   ↓
PUBLIC DISPLAY
  index.html ──fetch──→ talent-profiles.json ──→ renderProfiles() → profiles-grid

ACTIVE TALENT ADMIN
  admin-review.html ──fetch──→ talent-profiles.json ──→ Active Talent tab
                    ──queueAdminAction──→ apply-admin-actions.js
                                         ↓
                                   talent-profiles.json (written)

VALIDATION
  validate-public-data.js ──read──→ talent-profiles.json
                                  → check contact fields, icons, local edits
```

---

## Backend (Apps Script) — `Code.gs`

| Sheet | Constant | Action |
|-------|----------|--------|
| `Pending Talent` | `SHEETS.pendingTalent` | `submitTalent` writes here; `getPendingTalent` reads here; `approveTalent` moves to Approved |
| `Approved Talent` | `SHEETS.approvedTalent` | `getApprovedTalent` reads here |

---

## Remediation Plan

### Remove from `backend/dotgithub/workflows/jobs-sync.yml`:

| Section | Lines to Remove |
|---------|-----------------|
| `Detect all changes` (git status) | 95: `"$JOBS_DIR/pending-talent.json" \` and 96: `"$JOBS_DIR/pending-talent-profiles.json" \` |
| `Upload admin sync artifacts` | 131: `resources/jobs/pending-talent.json` and 132: `resources/jobs/pending-talent-profiles.json` |
| `Commit and push generated changes` | 158: `"$JOBS_DIR/pending-talent.json" \` and 159: `"$JOBS_DIR/pending-talent-profiles.json" \` |

### Preserve (already correct):

| Reference | Reason |
|-----------|--------|
| `talent-profiles.json` in CI (lines 94, 130, 157) | Canonial approved/talent file |
| `jobs:fetch-approved-talent` step (line 68) | Generates `talent-profiles.json` |
| `index.html` fetch of `talent-profiles.json` | Public display |
| `admin-review.html` LOCAL_TALENT_URL | Active Talent tab |
| `admin-review.html` backend calls (getPendingTalent) | Real-time pending queue |
| `validate-public-data.js` talent checks | Data integrity |
| `apply-admin-actions.js` talent operations | Admin edits |
