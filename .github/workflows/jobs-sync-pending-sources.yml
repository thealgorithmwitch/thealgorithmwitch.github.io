name: Jobs Sync Pending Sources

on:
  workflow_dispatch:
  schedule:
    - cron: "0 15 * * 3"

permissions:
  contents: write

concurrency:
  group: jobs-board-state
  cancel-in-progress: false

jobs:
  sync-pending-sources:
    runs-on: ubuntu-latest

    env:
      JOBS_DIR: resources/jobs

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install dependencies if needed
        shell: bash
        run: |
          if [ -f "$JOBS_DIR/package-lock.json" ] || [ -f "$JOBS_DIR/npm-shrinkwrap.json" ]; then
            npm --prefix "$JOBS_DIR" ci
          elif node -e "const p=require('./${JOBS_DIR}/package.json'); const hasDeps=Object.keys(p.dependencies||{}).length > 0 || Object.keys(p.devDependencies||{}).length > 0; process.exit(hasDeps ? 0 : 1)"; then
            npm --prefix "$JOBS_DIR" install
          else
            echo "No package dependencies declared. Skipping npm install."
          fi

      - name: Validate pending source sync scripts
        shell: bash
        run: |
          for file in \
            "$JOBS_DIR/scripts/sync-targeted-pending-sources.js" \
            "$JOBS_DIR/scripts/ats-clients.js" \
            "$JOBS_DIR/scripts/source-discovery-helpers.js"
          do
            if [ -f "$file" ]; then
              node --check "$file"
            else
              echo "Skipping missing optional script: $file"
            fi
          done

      - name: Guard and fix blocked sources before validation
        run: npm --prefix "$JOBS_DIR" run jobs:check-blocked-sources -- --write

      - name: Validate public data before pending sync
        run: npm --prefix "$JOBS_DIR" run jobs:validate

      - name: Run pending-only source sync
        run: npm --prefix "$JOBS_DIR" run jobs:sync-pending-sources

      - name: Guard and fix blocked sources before validation
        run: npm --prefix "$JOBS_DIR" run jobs:check-blocked-sources -- --write

      - name: Validate public data after pending sync
        run: npm --prefix "$JOBS_DIR" run jobs:validate

      - name: Check for pending/source-health changes
        id: changes
        shell: bash
        run: |
          CORE_CHANGED="$(git status --porcelain -- \
            "$JOBS_DIR/pending-synced-jobs.json" \
            "$JOBS_DIR/source-health-latest.json" \
            || true)"
          REPORT_CHANGED="$(git status --porcelain -- \
            "$JOBS_DIR/reports/targeted-pending-source-sync.json" \
            "$JOBS_DIR/reports/pending-source-state-audit.json" \
            "$JOBS_DIR/validation-snapshots/latest.json" \
            || true)"
          if [ -n "$CORE_CHANGED" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Detected pending/source-health changes:"
            echo "$CORE_CHANGED"
            if [ -n "$REPORT_CHANGED" ]; then
              echo "$REPORT_CHANGED"
            fi
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
            if [ -n "$REPORT_CHANGED" ]; then
              echo "Report-only pending sync changes detected; skipping commit."
              echo "$REPORT_CHANGED"
            else
              echo "No pending/source-health changes detected."
            fi
          fi

      - name: Upload pending sync artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jobs-sync-pending-sources-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            ${{ env.JOBS_DIR }}/pending-synced-jobs.json
            ${{ env.JOBS_DIR }}/pending-jobs.json
            ${{ env.JOBS_DIR }}/source-health-latest.json
            ${{ env.JOBS_DIR }}/source-discovery-candidates.json
            ${{ env.JOBS_DIR }}/scrape-report.json
            ${{ env.JOBS_DIR }}/reports/targeted-pending-source-sync.json
            ${{ env.JOBS_DIR }}/reports/pending-source-state-audit.json
            ${{ env.JOBS_DIR }}/validation-snapshots/latest.json

      - name: Commit and push pending/source-health changes
        if: steps.changes.outputs.changed == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          for path in \
            "$JOBS_DIR/pending-synced-jobs.json" \
            "$JOBS_DIR/pending-jobs.json" \
            "$JOBS_DIR/source-health-latest.json" \
            "$JOBS_DIR/source-discovery-candidates.json" \
            "$JOBS_DIR/scrape-report.json" \
            "$JOBS_DIR/reports/targeted-pending-source-sync.json" \
            "$JOBS_DIR/reports/pending-source-state-audit.json" \
            "$JOBS_DIR/validation-snapshots/latest.json"; do
            if [ -e "$path" ]; then
              git add "$path"
            else
              echo "Skipping missing path: $path"
            fi
          done
          if git diff --cached --quiet; then
            echo "No staged changes to commit."
            exit 0
          fi
          git commit -m "Sync pending-only ATS sources"
          git push

      - name: Report no-op
        if: steps.changes.outputs.changed != 'true'
        run: echo "No commit created."
